define([
    'libs/backbone'
], function () {
    var linkModel = Backbone.Model.extend({
        defaults: {
        },
        initialize: function () {
        },

    });
    return linkModel;

});
