require([
    'netmap/graph_view',
    'netmap/control_view',
    'netmap/graph'

], function (GraphView, ControlView, Graph) {

    $(function () {

        var graphView = new GraphView();
        var controlView = new ControlView();
    });
});