define([
    'libs/backbone'
], function () {
    var l3EdgeModel = Backbone.Model.extend({
        defaults: {
        },
        initialize: function () {
        },

    });
    return l3EdgeModel;

});
