define([
    'libs/backbone'
], function () {
    var nodeModel = Backbone.Model.extend({
        defaults: {

        },
        initialize: function () {
        }
    });
    return nodeModel;

});
