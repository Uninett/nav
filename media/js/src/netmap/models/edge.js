define([
    'libs/backbone'
], function () {
    var edgeModel = Backbone.Model.extend({
        defaults: {
        },
        initialize: function () {
        },

    });
    return edgeModel;

});
