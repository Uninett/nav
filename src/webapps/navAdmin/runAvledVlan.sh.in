#!/usr/bin/env bash

prefix=@prefix@
exec_prefix=@exec_prefix@
bindir=@bindir@
localstatedir=@localstatedir@
sysconfdir=@sysconfdir@
libdir=@libdir@
javalibdir=@javalibdir@

NAV_CONF="${sysconfdir}/nav.conf"

if test -z "$JAVA_HOME"; then 
    JAVA_HOME=`awk -F= '/JAVA_HOME/ && $1!~/#.*/{gsub("[\t ]", "", $2); print $2}' $NAV_CONF`
fi

CONF_FILE="${sysconfdir}/navAdmin.conf"
CUR_DIR="${javalibdir}/navAdmin"
REPORT_DIR="${localstatedir}/log"

cd $CUR_DIR
$JAVA_HOME/bin/java -cp "NavUtils.jar:$CLASSPATH" NavUtils -avledVlan > "$REPORT_DIR/avledVlan.html"
