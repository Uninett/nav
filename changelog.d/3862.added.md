Added support for logging in with OAUth2, OIDC and SAML via the dependency
`django-allauth`, the same that supports 2-factor authentication. Added
documentation for configuration via a backdoor: manipulating Django settings
directly, with examples for bundled OAuth2-provders and generic OIDC.
