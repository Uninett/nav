`nav stop`/`nav restart` now reliably terminate `ipdevpoll` running in multiprocess mode: the service manager escalates to `SIGKILL` and signals the whole process group when a graceful shutdown times out, and the ipdevpoll master forces its own exit if the reactor fails to shut down within `shutdown_timeout` seconds. This prevents hung masters and leftover `<defunct>` worker processes.
