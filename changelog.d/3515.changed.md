Replaced use of `re_path()` with `path()` where possible. The remaining
instances of `re_path()` should be looked over as several are not quite up to
spec: not ending with '$', or the regexp probably being too greedy etc.
