Made NAV's authorization system compatible with the LoginRequiredMiddleware
that was added in Django 5.1. A view can now be explicitly marked as not
requiring login (via `view.login_required=False`) , which means we can simplify
other parts of the authorization system.
