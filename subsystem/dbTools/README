This directory contains utilities for managing the NAV database.

dump.py      -- Dumps seed information from the database into text
                files which can be bulk imported through the editdb
                web interface.

navclean.py  -- Removes expired CAM/ARP records from the database to
                save disk space.

pg_backup.pl -- Makes backups of your entire PostgreSQL database and
                rotates backup files for specified periods of time.
