SHELL = @SHELL@
INSTALL = @INSTALL@
CHEETAH = @CHEETAH@

prefix = @prefix@
exec_prefix = @exec_prefix@
bindir = @bindir@
libdir = @libdir@
sysconfdir = @sysconfdir@

pythonlibdir = @pythonlibdir@
templatedir = ${pythonlibdir}/nav/web/templates
webroot = @webroot@
imagedir = ${webroot}/images/arnold
tooldir = @tooldir@

imagefiles = details.png enable.png

.PHONY: install-web

install: install-web

install-web:
	for file in $(imagefiles); do \
	  $(INSTALL) -v -D -m 644 $$file $(DESTDIR)$(imagedir)/$$file || exit 1; \
	done

clean:
	rm -f *~

distclean: clean
	rm -f Makefile
