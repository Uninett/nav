NAV smsd TODO
$Id$

- Fix all FIXMEs
- Make sure smsd removes pids and exits properly if HUP'ed or killed.
- General dispatcher class to inherit from
- A DispatcherError Exception so we can use several dispatchers in a
  prioritizedsequense with failover to the less wanted dispatchers
- Write unit tests
- Write unit tests that are ment to fail and trig the exceptions
- Create dispatcher for the Boost Communications API. Bjørn Ove got docs and
  Perl implementation.
- Write REAME and/or INSTALL
