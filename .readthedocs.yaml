version: 2

sphinx:
    configuration: doc/conf.py
    builder: html

build:
  os: ubuntu-22.04
  tools:
    python: "3.11"
  apt_packages:
    - libsnmp40

python:
  install:
    - method: uv
      command: sync
      groups:
        - docs
